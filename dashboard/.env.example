# Base URL for API calls. "/" = same origin the dashboard is served from (works from any device that
# opens the Pi dashboard). Leave "/" unless the dashboard is hosted somewhere other than the server.
VITE_APP_API_BASE_URL=/
